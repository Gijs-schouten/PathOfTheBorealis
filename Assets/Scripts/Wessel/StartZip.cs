/*using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class StartZip : MonoBehaviour
{
    public bool canzip = false;
    ZipMove ziplinescript;
    
    void Start()
    {
        ZipMove ziplinescript = GameObject.Find("Zipline").GetComponent<ZipMove>();
    }

  

void Update()
    {
        if (canzip == true && Input.GetKeyDown(KeyCode.Space))
        {
            //ziplinescript.UseZipLine();
        }
    }
}
*/